#include "main.h"
/**
 * _islower - verifies if a character is lowercase
 * @c: the character to be verified
 * Return: if c is a lowercase character, otherwise returns 0
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
