#include "3-calc.h"

int op_add(int p, int k);
int op_sub(int p, int k);
int op_mul(int p, int k);
int op_div(int p, int k);
int op_mod(int p, int k);


/**
 * op_add - Sums two numbers
 * @p: first input
 * @k: second input
 *
 * Return: The sum of p and k.
 */

int op_add(int p, int k)
{
		return (p + k);
}


/**
 * op_add - Sums two numbers
 * @p: first input
 * @k: second input
 *
 * Return: The sum of p and k.
 */

int op_sub(int p, int k)
{
		return (p - k);
}

/**
 * op_add - Sums two numbers
 * @p: first input
 * @k: second input
 *
 * Return: The sum of p and k.
 */

int op_mul(int p, int k)
{
	return (p * k);
}

/**
 * op_add - Sums two numbers
 * @p: first input
 * @k: second input
 *
 * Return: The sum of p and k.
 */


int op_div(int p, int k)
{
		return (p / k);
}

/**
 * op_add - Sums two numbers
 * @p: first input
 * @k: second input
 *
 * Return: The sum of p and k.
 */

int op_mod(int p, int k)
{
		return (p % k);
}

